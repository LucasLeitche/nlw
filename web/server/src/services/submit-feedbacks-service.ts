interface SubmitFeedbackServiceRequest{
    type: string;
    comment: string;
    screenshot?: string;
}

export class SubmitFeedbackService{
    // Executar a criação de um novo feedback
    async execute(request : SubmitFeedbackService){
        
    }
}